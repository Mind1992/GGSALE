<!DOCTYPE html>
<html class="all">
<head>
  <title>Gigisale</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <link href='http://fonts.googleapis.com/css?family=Istok+Web:700' rel='stylesheet' type='text/css'>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
  <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
  <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
  <![endif]-->
</head>
<body class="all">
  <nav class="navbar navbar-default navbar-fixed-top " role="navigation">
    <div class="container" id="all-wrapper">
      <div class="row">
        <div class="col-md-5">
          <%= link_to "GGSALE", root_path, id: "logo-all" %>
          <%= form_tag search_sales_path, class: "navbar-form", role: "search", method: :get do %>
            <div class="input-group">
              <%= text_field_tag :address, params[:address], class: "form-control", placeholder: "Where are you looking for?", id: "all-search" %>
              <div class="input-group-btn">
                <%= submit_tag "Search", class: "btn", id: "all-search-btn", 'data-no-turbolink' => true %>
              </div>
            </div>
          <% end %>
        </div>
      <div class="col-md-7">
        <% if user_signed_in? %>
        <div class="navbar-right">
          <p class="navbar-text"><%= current_user.username %></p>
          <% if user_signed_in? %>
            <%= link_to "Add Sale", new_sale_path, id: "new_sale", class: "btn btn-success navbar-btn", remote: true %>
            <%= link_to "Manage Sales", user_path(current_user), id: "user_sales", class: "btn btn-success navbar-btn" %>
          <% end %>
          <%= link_to 'Edit profile', edit_user_registration_path, class: "btn btn-default navbar-btn" %>
          <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "btn btn-default navbar-btn all-session-links" %>
        </div>
        <% else %>
          <div class="navbar-right session-links">
            <%= link_to "Sign Up", new_user_registration_path, class: "session-links nav-links all-links" %>
            <%= link_to "Log In", new_user_session_path, class: "session-links nav-links all-links" %>
          </div>
        <% end %>
      </div>
    </div>
  </nav>
<% if notice %>
	<p class="notice alert alert-success" role="alert"><%= notice %></p>
<% elsif alert %>
	<p class="alert alert-danger" role="alert"><%= alert %></p>
<% end %>
<%= yield %>
</body>
</html>
